package com.yunat.ccms.channel.support.service;

import com.yunat.ccms.channel.support.domain.ChannelLogger;


public interface ChannelLoggerService {
	void saveOrUpdate(ChannelLogger channelLogger);
}
